# Handoff — 2026-06-22 (social system consolidation + posting workflow)

**`ai-social-system` is the single source of truth for the whole social system.**
Posting/scheduling = **Blotato** (never Buffer). Video = **16:9 only**.

## What shipped today (open PRs)

| PR | What |
|---|---|
| #18 | Merge `Youtube-system` repo → `ai-social-system/youtube/` (Buffer→Blotato, auto-gen claim killed) |
| #2 (Youtube-system) | Tombstone closing the Youtube-system repo (merge AFTER #18, then archive) |
| #19 | The 6 creative skills → `creative-skills/` (preserve all, delete nothing) |
| #20 | `make-thumbnail` — deterministic 1280×720 thumbnails (real photo + code, no AI) |
| #21 | `research-content` — the real first step (research before drafting) |
| #17 | `package-video` + **quality gate** (clone check + pre-publish checks) |

Also: The Truth reorg (lean flyover at `~/Desktop/Truth/`; detail in the repo;
`the-truth-DEPRECATED/` retired). Old clone registry's 9:16/Buffer defaults corrected.

## The workflow (settled order)

```
1. SCHEDULE — what's OWED?  (cadence; YouTube is the drift — not in its owed-calc yet)
2. RESEARCH — research-content (idea bank → sourced brief)
3. SCRIPT/COPY — write-content (soul voice)
4. VIDEO = an APPROVED finished clip — NEVER render (all renders rejected)
   ── GATE A: approved look · 16:9 · audio · clean · matches
5. package-video (captions once + name-only lower-third + CTA) → make-thumbnail
   ── GATE B: captions · name-only · CTA · thumbnail · compliance
   ── HUMAN GATE: stage PRIVATE → Brent reviews → Brent flips public
6. cadence/blotato schedule (Blotato, 16:9)
```

## The ONLY two approved clones (never generate a new one)

- **Blue-polo** `a1d0c5d8d87f4d358769ab216cbe7398` — 16:9, **~8s, a render TEST (not real content)**
- **3 Tools** `1adc7ebdf9b54e0fa3d94b129798a85d` — 16:9 1080p, **~5 min, the real content clip**

Source of truth = HeyGen "Brent clone 2" folder. **No avatar generation, any engine.**

## ⏭️ EXACT pending action (awaiting Brent's word)

Package an approved clip → GATE A → captions + name-only lower-third + CTA →
`make-thumbnail` → GATE B → stage **PRIVATE** → Brent flips public. Fills the **owed
YouTube slot** (FB/IG/X are scheduled through 2026-06-25; YouTube has nothing).

**Open decision:** Brent said "pick blue shirt" — but blue-polo is an **8-second test
render**, not real content. Flagged. Choice: post the 8s blue-shirt as-is, OR use the
**3 Tools** ~5-min real clip.

**IMPORTANT:** since we post an EXISTING clip (no render), the **title / description /
thumbnail must match the CLIP's actual content** (3 Tools = AI tools), NOT the
follow-up "silence/2-vs-7" script written earlier (that script was for a render we are
NOT doing — parked at `~/Desktop/script-followup-silence-2026-06-22.md`).
