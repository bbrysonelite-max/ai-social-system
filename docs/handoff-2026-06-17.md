# Handoff — ai-social-system (Brent's content factory)

Date: 2026-06-17
Repo: `github.com/bbrysonelite-max/ai-social-system` (private) · local clone `/Users/brentbryson/ai-social-system`

## Mission (one line)
Programmatically post valuable, on-voice content about Tiger Claw + "AI for distribution" across Brent's connected social accounts via Blotato — for brand/authority, the webinar funnel, and warming his own team. Adopts Sabrina Ramonov's (Blotato founder) content-factory model: write strong cores in voice → repurpose/schedule via Blotato → weekly batch, human-in-loop.

## Read these first (don't duplicate — they hold the detail)
- Spec: `ai-social-system/docs/DESIGN.md` (merged PR #1)
- Plan: `ai-social-system/docs/superpowers/plans/2026-06-17-write-content-skill.md` (merged PR #2)
- Warm-up plan: `ai-social-system/docs/account-warm-up-plan.md` (PR #4 — **OPEN, awaiting Brent's merge**)
- Memory: `~/.claude/projects/-Users-brentbryson/memory/project_blotato_social_posting.md` and `project_jeff_mack_upline.md`

## State — DONE
- **Skill 1 `write-content` BUILT + MERGED to main** (PR #3). Drafts platform-specific posts in Brent's voice; FB/IG primary, YouTube/X on request; self-review rubric (Voice/Compliance/Frame/Platform/CTA) + a batch cadence-variety guard (prevents the "same two sentences over and over" failure Brent explicitly worries about). Files: `skills/write-content/SKILL.md` + `references/{voice,pillars-and-audience,cta-and-funnel,never-say,platform-formats,examples}.md`.
- **Idea bank:** `~/Desktop/Social Media Posts: Ideas/` — ~76 team-first ideas mined from `~/Desktop/Infographics`, plus `repurposing-queue.md` (YouTube videos/shorts/thumbnails) and `00-INDEX.md`.
- **Blotato MCP:** USER/GLOBAL scope, ✔ Connected (loads in every folder). Higgsfield MCP was evaluated and **DROPPED** (OAuth-only, not needed — Blotato's own Nano Banana image-gen + existing assets cover visuals).
- Desktop tidied: `Infographic 2` merged into `Infographics`; empty `Infographics/Infographics New` is the staging folder for new infographics.

## State — BLOCKED on a session RESTART
The Blotato MCP tools are connected but **only load on a fresh `claude` session** (verified NOT loaded in the session that built this). Everything below needs the restart:
- **Task #4:** verify Brent's connected channels (he says: Facebook=biggest, Instagram=2nd, X=tiny, YouTube; no LinkedIn/TikTok) against the live Blotato dashboard, and map the Blotato tool surface (publish / media-gen / schedule / repurpose).
- **Skill 2 `blotato-post`** (stub exists at `skills/blotato-post/SKILL.md`): takes `write-content` drafts → Blotato **repurposing** engine (Brent chose Blotato specifically for repurposing) + visuals + scheduling. Human review gate before anything schedules. Respect the warm-up ramp.

## State — open / deferred
- **Task #6:** content idea database proper — deferred; the Desktop folder is the DB "for now" (graduate into `content-db/` when big).
- **Webinar workstream:** PARKED (no webinar skill; build lives in `bbrysonelite-max/Tiger-Webinar-Funnel`). Candidate Skill 3, team-first reframe.
- **YouTube scripts not yet mined:** `~/Desktop/YOUTUBE/script_v1_5-ai-tools-network-marketer.md` + `script_v2_HEYGEN_READY.md` — offered to mine into the idea bank; not done.

## Key decisions (locked — don't relitigate)
- **Messaging spine:** "What will Tiger do for your team?" — team-leverage, safe/duplicable AI, follow-up+coaching.
- **Audience filter:** team-first LEADERS (Brent's words: "people who think about their team, not themselves"). Repel me-first.
- **CTA:** primary = high-value lead magnet → email (existing "Factory Blueprint" → "Agentic Loadout" newsletter, Stan Store + Beehiiv). Secondary = a conversation (NOT a sale) for leaders/white-label. Never cold-checkout-link Tiger. Warm-up: reach-safe (comment-to-get / DM keyword), not raw links.
- **Compliance:** biography, never earnings. No income/guarantee/health claims; no company-as-endorsing. (See `references/never-say.md`.)
- This is brand/visibility (incl. Brent's team), NOT the cold content-led lead gen he rejected in 2026-04 — high-VALUE magnets on earned proof.

## ⚠️ Security (action for Brent, not the agent)
Two secrets leaked into the prior transcript and should be ROTATED: the **Blotato API key** (currently in `~/.claude.json` user-scope MCP header) and a **Higgsfield secret** (in `~/Desktop/GitSync/kloop.env`). After Brent rotates the Blotato key, swap the fresh value into the global-scope MCP config. Do NOT echo secret values.

## Working norms (Brent)
- Global rules: never push to main directly (feature branch + PR); verify merges with `gh pr view`; honesty over false confidence; build only what's asked.
- He wants disciplined, step-by-step work — spec → plan → build, with gates. He flagged a "rebuild-it-twice / slop" habit; reuse existing assets, don't rebuild.
- Three-agent rule: author / test / review by separate agents (subagent-driven-development satisfied this for Skill 1).
- Keys live in `~/Desktop/GitSync/`. Voice canon: `Youtube-system/identity/*` + `~/Desktop/vault-personal/.../voice-cadences.md` + the "Third Window" manifesto. (NOT `soul-voice-block.md` — that's Tiger's 1:1 agent voice.)
- Separate context: he is also onboarding **Jeff Mack (his upline)** into Tiger Claw — he is handling that himself; stay on the social/content work unless he redirects.

## Suggested next step (no restart needed)
Test-drive `write-content`: take one idea from the idea bank, draft FB + IG posts, run the self-review gate — proves the writing engine before distribution is wired. OR mine the 2 YouTube scripts. OR (Brent) merge PR #4 + rotate the Blotato key + restart.

## Suggested skills for the next agent
- **superpowers:subagent-driven-development** — to build Skill 2 (`blotato-post`) from a plan (author/review/fix separated). Plan it first with **superpowers:writing-plans**, brainstorm scope with **superpowers:brainstorming** if unclear.
- **mine** skill — when Brent says "mine the new ones" (new infographics in `Infographics New`) or to mine the YouTube scripts into the idea bank.
- **tigerclaw-daily-checks** — only if work shifts to the Tiger Claw product (e.g., Jeff Mack onboarding).
- **closing-ritual** — at session end to reconcile docs/memory.
